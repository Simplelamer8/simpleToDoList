import { createContext } from "react";

const TasksContext = createContext(null);
const TabsContext = createContext(null);
const TaskOptionContext = createContext(null);

export {TasksContext};
export {TabsContext};
export {TaskOptionContext};